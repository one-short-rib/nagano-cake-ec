<!-- recieves order -->

<table class="table table-bordered">
  <tr>
    <th>商品合計</th>
    <td><%= total_price(current_member.cart_items).to_s(:delimited) %></td>
  </tr>
  <tr>
    <th>配送料</th>
    <td><%= order.postage %></td>
  </tr>
  <tr>
    <th>ご請求額</th>
    <td><%= order.billing_amount.to_s(:delimited) %></td>
  </tr>
</table>
