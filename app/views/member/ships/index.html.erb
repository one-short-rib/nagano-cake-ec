<h1>Member::Ships#index</h1>
<p>Find me in app/views/member/ships/index.html.erb</p>

<div class="row">
	<div class="col-xs-10 col-xs-offset-1">
		<h4>　配送先登録 / 一覧　</h4>
			<%= form_for @ship, url: '/members/ships' do |f| %>
				<div class="form_body">
					<table>
						<tr>
							<td><%= f.label :postal_code,"郵便番号(ハイフンなし)" %></td>
							<td><%= f.text_field :postal_code, class: "postal_code" %></td>
						</tr>
						<tr>
							<td><%= f.label :address,"住所" %></td>
							<td colspan="2"><%= f.text_field :address, class: "address" %></td>
						</tr>
						<tr>
							<td><%= f.label :name,"宛名" %></td>
							<td><%= f.text_field :name, class: "name" %></td>
							<td>&nbsp;</td>
							<td><%= f.submit "登録する", class: "btn btn-success" %></td>
						</tr>
					</table>
				</div>
			<% end %>
			<!-- ships一覧 -->
			<div class="row ships_index">
				<table>
					<thead>
						<tr>
							<th>郵便番号</th>
							<th>住所</th>
							<th>宛名</th>
							<th colspan="2"></th>
						</tr>
					</thead>
					<tbody>
						<% if @ships.present? %>
							<% @ships.each do |ship| %>
								<%= render 'member/ships/ship', ship: ship %>
							<% end %>
						<% else %>
						 <tr>
						 	<td colspan="5">登録された配送先はありません</td>
						 </tr>
					 <% end %>
					</tbody>
				</table>
			</div>
		</div>
	</div>
	<div class="col-xs-1"></div>
</div>
