<% if @amount == '0' %>
    $("#delete-cart-<%= @cart_item.id.to_s %>").empty();
<% else %>
    $("#cart-subtotal-<%= @cart_item.id.to_s %>").html("<%= @subtotal %>");
<% end %>

$("#flash").html("<%= j(render 'layouts/flash') %>");
