<!-- recieves cart_item -->

<div class="cart_button">
  <%= form_for(cart_item, url: members_cart_items_path) do |f| %>
        <%= f.number_field :amount, min: 1, max: 50, placeholder:"個数選択" %>
        <%= f.submit "カートに入れる", class:"btn btn-info" %>
  <% end %>
</div>
