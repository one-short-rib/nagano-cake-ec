<!-- recieves items -->

<% items.each do |item| %>
  <div class="item-box">
    <%= attachment_image_tag item, :item_image, format: 'jpg', fallback: "no_image.jpg", size: "250x200" %><br>
    <%= item.name %><br>
    <%= "¥#{item.price.to_s(:delimited)}" %>
  </div>
<% end %>
