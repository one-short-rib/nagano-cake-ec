<!-- recieves items -->

<% items.each do |item| %>
  <div class="item-box">
    <%= attachment_image_tag item, :item_image, :fill, 50, 50 %>
    <%= item.name %>
    <%= item.price.to_s(:delimited) %>
  </div>
<% end %>
