<!-- recieves items -->

<% items.each do |item| %>
  <div class="item-box pull-left">
    <%= link_to item_path(item) do %>
    <%= attachment_image_tag item, :item_image, format: 'jpg', fallback: "sample3.jpg", size: "180x140" %>
    <p><%= item.name %></p>
    <p><%= "¥#{item.price.to_s(:delimited)}" %></p>
    <% end %>
  </div>
<% end %>
